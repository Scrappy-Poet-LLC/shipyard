# Application URL (required for production). Used for magic link redirects in confirmation emails.
# In production, set to your app domain (e.g. https://app.example.com).
# Also update Supabase Dashboard → Authentication → URL Configuration → Site URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase (local defaults from `supabase start` output)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key-from-supabase-start-output>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key-from-supabase-start-output>

# GitHub App
GITHUB_APP_ID=your-app-id
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
GITHUB_WEBHOOK_SECRET=your-webhook-secret
